A real-time chat web application where you can chat with anyone in the same room as you.

To run the project, clone the repository, install Maven dependencies, and then run ChatgoApplication.java. The application will run on port 8080.

If you want to test out chatting with multiple users, create a new tab in your browser, and it will act as another user.
